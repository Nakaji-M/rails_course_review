```
CREATE TABLE `form` (
  `id`         CHAR(36)     NOT NULL PRIMARY KEY DEFAULT (UUID()),
  `createdAt`  DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updatedAt`  DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `year`       INT          NOT NULL,
  UNIQUE KEY `uniq_form_year` (`year`)
) ENGINE=InnoDB
  DEFAULT CHARSET=utf8mb4
  COLLATE=utf8mb4_unicode_ci;
```

```
CREATE TABLE `question` (
  `id`           CHAR(36)              NOT NULL PRIMARY KEY DEFAULT (UUID()),
  `textJa`       TEXT                  NOT NULL,
  `textEn`       TEXT                  NOT NULL,
  `descriptionJa` TEXT                 NOT NULL,
  `descriptionEn` TEXT                 NOT NULL,
  `placeholderJa` VARCHAR(255)         NOT NULL,
  `placeholderEn` VARCHAR(255)         NOT NULL,
  `formId`       CHAR(36)              NOT NULL,
  `type`         TEXT                 NOT NULL,
  `require`      BOOLEAN               NOT NULL DEFAULT FALSE,
  `filterType`   INT                   NULL,
  `order`        INT                   NOT NULL,
  `createdAt`    DATETIME              NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updatedAt`    DATETIME              NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX (`formId`),
  CONSTRAINT `fk_question_form`
    FOREIGN KEY (`formId`) REFERENCES `form`(`id`)
) ENGINE=InnoDB
  DEFAULT CHARSET=utf8mb4
  COLLATE=utf8mb4_unicode_ci;
```

optionにidは不要？

```
CREATE TABLE `option` (
  `questionId`  CHAR(36)     NOT NULL,
  `textJa`      TEXT         NOT NULL,
  `textEn`      TEXT         NOT NULL,
  `order`       INT          NOT NULL,
  `filterType`  INT          NOT NULL,
  `createdAt`   DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updatedAt`   DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX `idx_option_question` (`questionId`),
  CONSTRAINT `fk_option_question`
	FOREIGN KEY (`questionId`) REFERENCES `question`(`id`)
) ENGINE=InnoDB
  DEFAULT CHARSET=utf8mb4
  COLLATE=utf8mb4_unicode_ci;
```

```
CREATE TABLE `department` (
  `id`   CHAR(36)     NOT NULL PRIMARY KEY DEFAULT (UUID()),
  `name` VARCHAR(255) NOT NULL,
  `createdAt`  DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updatedAt`  DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB
  DEFAULT CHARSET=utf8mb4
  COLLATE=utf8mb4_unicode_ci;
```

```
CREATE TABLE `course` (
  `id`             CHAR(36)     NOT NULL PRIMARY KEY DEFAULT (UUID()),
  `departmentId`   CHAR(36)     NOT NULL,
  `ocwId`          INT          NOT NULL,
  `courseNumber`   VARCHAR(100) NOT NULL,
  `titleJa`        VARCHAR(255) NOT NULL,
  `titleEn`        VARCHAR(255) NOT NULL,
  `year`           INT          NOT NULL,
  `startQuarter`   INT          NOT NULL,
  `endQuarter`     INT          NOT NULL,
  `createdAt`  DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updatedAt`  DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX `idx_cource_department` (`departmentId`),
  CONSTRAINT `fk_cource_department`
    FOREIGN KEY (`departmentId`) REFERENCES `department`(`id`)
) ENGINE=InnoDB
  DEFAULT CHARSET=utf8mb4
  COLLATE=utf8mb4_unicode_ci;
```

```
CREATE TABLE `course_department` (
  `courseId`     CHAR(36) NOT NULL,
  `departmentId` CHAR(36) NOT NULL,
  `createdAt`  DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updatedAt`  DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`courseId`,`departmentId`),
  INDEX `idx_cd_course`     (`courseId`),
  INDEX `idx_cd_department` (`departmentId`),
  CONSTRAINT `fk_cd_course`
    FOREIGN KEY (`courseId`)     REFERENCES `cource`   (`id`),
  CONSTRAINT `fk_cd_department`
    FOREIGN KEY (`departmentId`) REFERENCES `department`(`id`)
) ENGINE=InnoDB
  DEFAULT CHARSET=utf8mb4
  COLLATE=utf8mb4_unicode_ci;
```

```
CREATE TABLE `teacher` (
  `id`   CHAR(36)      NOT NULL PRIMARY KEY DEFAULT (UUID()),
  `name` VARCHAR(255)  NOT NULL,
  `createdAt`  DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updatedAt`  DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB
  DEFAULT CHARSET=utf8mb4
  COLLATE=utf8mb4_unicode_ci;
```

```
CREATE TABLE `course_teacher` (
  `courseId`  CHAR(36) NOT NULL,
  `teacherId` CHAR(36) NOT NULL,
  `createdAt`  DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updatedAt`  DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`courseId`, `teacherId`),
  INDEX `idx_ct_course`  (`courseId`),
  INDEX `idx_ct_teacher` (`teacherId`),
  CONSTRAINT `fk_ct_course`
    FOREIGN KEY (`courseId`)  REFERENCES `cource`  (`id`),
  CONSTRAINT `fk_ct_teacher`
    FOREIGN KEY (`teacherId`) REFERENCES `teacher` (`id`)
) ENGINE=InnoDB
  DEFAULT CHARSET=utf8mb4
  COLLATE=utf8mb4_unicode_ci;
```

```
CREATE TABLE `answer` (
  `formToken`  VARCHAR(255) NOT NULL,
  `courseId`   CHAR(36)     NOT NULL,
  `questionId` CHAR(36)     NOT NULL,
  `content`    TEXT         NOT NULL,
  `createdAt`  DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updatedAt`  DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP 
                 ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY `uniq_answer_token_course_question` (`formToken`,`courseId`,`questionId`),
  INDEX      `idx_answer_course`   (`courseId`),
  INDEX      `idx_answer_question` (`questionId`),
  CONSTRAINT `fk_answer_course`
    FOREIGN KEY (`courseId`)   REFERENCES `course`  (`id`),
  CONSTRAINT `fk_answer_question`
    FOREIGN KEY (`questionId`) REFERENCES `question`(`id`)
) ENGINE=InnoDB
  DEFAULT CHARSET=utf8mb4
  COLLATE=utf8mb4_unicode_ci;
```

